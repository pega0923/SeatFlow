# SeatFlow Project Specification

## 1. 프로젝트 개요

### 프로젝트명
**SeatFlow** (임시명)

### 한 줄 설명
대학교 도서관, 열람실, 스터디 공간, 회사 자율좌석제 등에서 사용할 수 있는 **좌석 예약 및 공정 배정 시스템**.

### 프로젝트 목적
이 프로젝트의 1차 목적은 **IT 산업기능요원 신입 백엔드 개발자 지원에 사용할 수 있는 포트폴리오**를 빠르게 완성하는 것이다.

단순 CRUD 게시판이나 흔한 예약 서비스에 그치지 않고 다음과 같은 실제 백엔드 문제를 직접 설계하고 해결하는 것을 목표로 한다.

- 동일 자원에 대한 동시 예약
- 시간 구간 충돌
- 한정된 좌석의 공정한 배정
- 반복 일정 처리
- 대기열 및 자동 재배정
- 통계 집계와 성능 최적화
- 인증/인가
- 캐싱
- 테스트
- 배포

최종적으로는 “모든 코드를 처음부터 직접 작성했다”가 아니라 다음을 할 수 있는 상태를 목표로 한다.

1. 프로젝트 전체 구조와 요청 흐름을 이해한다.
2. 핵심 비즈니스 로직을 자신의 말로 설명할 수 있다.
3. AI가 생성한 코드를 읽고 수정할 수 있다.
4. 오류가 발생했을 때 원인을 추적하고 해결 방향을 판단할 수 있다.
5. 기술 선택의 이유와 트레이드오프를 면접에서 설명할 수 있다.

---

## 2. 개발자 현재 상태

- 정보통신공학 계열 전공
- 정보처리기사 보유
- 백엔드 실무 경험은 거의 없음
- Java / Spring Boot / JPA / MySQL 등을 프로젝트를 진행하면서 익힐 예정
- Redis / Docker / 클라우드 등은 프로젝트 진행 후반에 실제 필요성이 생길 때 추가
- 취업 준비 시간이 촉박하므로 기초 이론을 장기간 선행 학습하는 방식은 사용하지 않음
- AI를 적극적으로 활용하여 동작하는 코드를 빠르게 만든 뒤, 해당 코드를 이해하고 수정 가능한 수준까지 학습하는 방식을 사용

---

## 3. 개발 방식: AI-First Implementation

SeatFlow는 **AI가 먼저 구현하고 사용자가 이해를 따라잡는 방식**으로 개발한다.

기본 사이클은 다음과 같다.

```text
기능 목표 정의
    ↓
AI가 최소 범위의 동작 코드 구현
    ↓
빌드 / 실행 / 테스트
    ↓
사용자가 코드 구조와 요청 흐름 이해
    ↓
핵심 개념과 기술 선택 이유 학습
    ↓
필요한 수정 또는 작은 직접 변경
    ↓
다음 기능으로 진행
```

### 기본 원칙

- 사용자가 Java/Spring의 모든 기초를 공부한 뒤 프로젝트를 시작하지 않는다.
- 현재 기능 구현에 필요한 코드와 설정은 AI가 우선 작성할 수 있다.
- AI는 한 번에 프로젝트 전체를 완성하지 않고 현재 Phase에 필요한 최소 범위만 구현한다.
- 각 단계가 끝날 때 사용자는 최소한 해당 코드의 역할과 요청 흐름을 설명할 수 있어야 한다.
- 사용자는 모든 코드를 암기할 필요는 없지만 핵심 클래스와 비즈니스 로직은 이해해야 한다.
- AI가 만든 코드라도 사용자가 설명할 수 없는 상태라면 해당 부분은 학습 대상으로 간주한다.

### AI에게 맡겨도 되는 작업

- 프로젝트 초기 골격 생성
- Gradle 설정
- 의존성 추가
- Entity / DTO / Controller / Service / Repository 생성
- 반복적인 CRUD 코드
- 테스트 코드 초안
- 설정 파일 작성
- 빌드 및 테스트 실행
- 오류 로그 분석
- 리팩터링 초안
- 문서화 초안

### 사용자가 반드시 이해해야 하는 부분

- HTTP 요청이 어떤 경로로 처리되는지
- Controller / Service / Repository의 책임
- 주요 Entity와 관계
- 핵심 비즈니스 규칙
- 예약 충돌 판정
- 포인트 추첨 알고리즘
- 트랜잭션과 동시성 문제
- 인증/인가 흐름
- 성능 문제가 왜 발생했고 어떻게 개선했는지
- Redis / 인덱스 / 락 등을 왜 사용했는지

---

## 4. 핵심 문제 정의

SeatFlow의 핵심 문제는 단순히 “빈 좌석을 예약한다”가 아니다.

> **한정된 좌석을 사용자들에게 어떻게 공정하고 효율적으로 배분할 것인가?**

이를 중심으로 일반 예약, 시간표 기반 반복 예약, 포인트 기반 추첨, 대기열, 통계 등의 기능을 확장한다.

---

## 5. 핵심 기능

### 5.1 기본 좌석 예약

- 좌석 목록 조회
- 특정 시간대 예약 가능 좌석 조회
- 좌석 예약
- 예약 취소
- 자신의 예약 내역 조회
- 이미 예약된 시간대와 겹치는 예약 차단

### 5.2 시간표 기반 반복 예약

대학 시간표처럼 반복 일정을 등록할 수 있다.

예:
- 월요일 13:00 ~ 15:00
- 수요일 10:00 ~ 12:00
- 금요일 14:00 ~ 16:00

고려할 문제:
- 기존 예약과의 충돌
- 일부 날짜만 예약 실패하는 경우
- 반복 일정 수정/삭제
- 특정 제외일 처리

### 5.3 포인트 기반 인기 좌석 추첨

인기 좌석에 여러 사용자가 몰리는 경우 단순 선착순 대신 **포인트 가중치 기반 추첨**을 사용한다.

예:
- A: 50P
- B: 30P
- C: 20P

기본적으로 투입 포인트 비율이 추첨 가중치가 된다.

정책 후보:
- 당첨자 포인트 차감
- 탈락자 포인트 전액 또는 일부 반환
- 주기적 기본 포인트 지급
- 연속 탈락자 보정
- 최근 당첨자 보정
- 노쇼 패널티

이 기능은 프로젝트의 대표 비즈니스 로직이며 단순 랜덤 처리보다 **공정성 정책과 설계 이유**를 설명할 수 있어야 한다.

### 5.4 대기열 및 자동 재배정

원하는 좌석이 이미 예약된 경우 대기 신청을 할 수 있다.

기존 예약자가 취소하면:
1. 대기열 확인
2. 정해진 우선순위 정책 적용
3. 다음 사용자에게 자동 배정 또는 예약 기회 제공

### 5.5 좌석 평점 및 리뷰

실제로 해당 좌석을 이용한 사용자만 평가할 수 있도록 한다.

좌석별 정보 예:
- 평균 평점
- 이용 횟수
- 평균 이용 시간
- 예약 경쟁률
- 콘센트 여부
- 창가 여부
- 조용함
- 냉방 체감
- 자유 리뷰

### 5.6 좌석 검색 및 필터링

예:
- 특정 시간대 예약 가능
- 평점 4.5 이상
- 콘센트 있음
- 창가 좌석
- 경쟁률 낮음
- 이용 횟수 많음

### 5.7 통계

- 가장 많이 사용된 좌석
- 경쟁률이 높은 좌석
- 평균 평점이 높은 좌석
- 시간대별 좌석 이용률
- 요일별 이용률
- 평균 이용 시간
- 노쇼율
- 인기 좌석 TOP N

통계 기능은 SQL 집계, 인덱스, 캐싱 등의 실전 학습 재료로 활용한다.

### 5.8 관리자 기능

- 좌석 등록/수정/삭제
- 전체 예약 현황 조회
- 사용자 관리
- 노쇼/패널티 관리
- 좌석 이용률 및 통계 조회

---

## 6. 프로젝트에서 다룰 백엔드 핵심 주제

### 데이터베이스
- 관계형 데이터 모델링
- Primary Key / Foreign Key
- 엔티티 관계 설계
- JOIN
- GROUP BY
- COUNT / AVG
- 인덱스
- 쿼리 성능 비교

### 예약 로직
- 시간 구간 중복 판정
- 예약 상태 관리
- 취소 처리
- 반복 일정
- 대기열

### 동시성
동일 좌석과 동일 시간대에 여러 예약 요청이 동시에 들어오는 문제를 실제로 재현한다.

검토 대상:
- 트랜잭션
- DB 제약조건
- 낙관적 락
- 비관적 락

기술을 먼저 넣지 않고 **문제를 재현한 뒤 해결책을 선택한다.**

### 인증과 인가
- 회원가입
- 로그인
- 일반 사용자 / 관리자 권한
- Spring Security
- 필요 시 JWT

### 성능
- 반복 집계 쿼리
- 인덱스 적용 전후 비교
- Redis 캐싱
- 캐시 만료 및 무효화 정책

### 안정성
- 예외 처리
- 입력 검증
- 단위 테스트
- 통합 테스트
- 예약/추첨 로직 테스트
- 동시성 테스트

---

## 7. 기본 기술 스택

### 초기 단계
- Java 21
- Spring Boot
- Gradle
- Spring Data JPA
- MySQL
- REST API
- Git
- GitHub

### 이후 필요할 때 추가
- Spring Security
- JWT
- Redis
- Docker
- Swagger / OpenAPI
- AWS 또는 다른 클라우드 서비스
- 테스트/부하 테스트 도구

### 기술 도입 원칙
기술은 포트폴리오에 이름을 추가하기 위해 사용하지 않는다.

잘못된 접근:
> Redis를 사용한 경험을 만들기 위해 Redis를 넣는다.

원하는 접근:
> 인기 좌석 통계에서 반복적인 집계 쿼리가 발생하는 것을 확인하고 DB 부하를 줄이기 위해 Redis 캐시를 적용한다.

---

## 8. 초기 데이터 모델 초안

### User
- id
- email
- password
- point
- role

### Seat
- id
- seatNumber
- location
- hasOutlet
- nearWindow
- status

### Reservation
- id
- userId
- seatId
- startTime
- endTime
- status
- createdAt

추후 필요에 따라 다음을 추가한다.
- Review
- PointHistory
- LotteryApplication
- WaitingEntry
- RecurringSchedule
- NoShowHistory

초기부터 모든 엔티티를 만들지 않는다.

---

## 9. 기본 서버 구조

```text
Client
  ↓ HTTP Request
Controller
  ↓
Service
  ↓
Repository
  ↓
Database
```

- **Controller**: HTTP 요청을 받고 응답을 반환한다.
- **Service**: 핵심 비즈니스 로직을 처리한다.
- **Repository**: DB 조회 및 저장을 담당한다.

AI가 코드를 생성하더라도 사용자는 최소한 각 계층의 역할과 요청 흐름을 이해해야 한다.

---

## 10. 개발 단계

### Phase 0 — 개발환경 및 프로젝트 생성

사용자가 모든 도구를 직접 세팅하는 것을 전제로 하지 않는다.

Codex 등의 AI 개발 에이전트를 활용해 다음을 우선 확인한다.
- Java 설치 여부
- Git 설치 여부
- 필요한 실행 환경
- 프로젝트 디렉터리 상태

필요한 환경을 준비한 뒤 AI가 Spring Boot 프로젝트 골격을 생성한다.

이 단계에서는 Java/Spring 선행학습을 길게 하지 않는다.

### Phase 1 — SeatFlow v0

최초 목표:

```http
GET /api/seats
```

요청 시 DB에 저장된 좌석 목록을 JSON으로 반환한다.

AI가 우선 구현할 것:
- Spring Boot 프로젝트
- MySQL 연결
- Seat Entity
- Seat Repository
- Seat Service
- Seat Controller
- 기본 테스트

사용자가 구현 후 이해할 것:
- `@Entity`가 무엇인지
- Repository가 어떤 역할을 하는지
- Controller → Service → Repository → DB 흐름
- JSON 응답이 어떻게 만들어지는지

### Phase 2 — 기본 예약
- 회원 기본 구조
- 좌석 등록/조회
- 예약 생성
- 예약 취소
- 내 예약 조회

### Phase 3 — 예약 시간 충돌
- 같은 좌석의 시간 중복 판정
- 잘못된 시간 범위 검증
- 예약 상태
- 예외 처리

### Phase 4 — 인증
- 회원가입
- 로그인
- Spring Security
- 인증 사용자만 예약
- 관리자 권한 분리
- 필요 시 JWT

### Phase 5 — 반복 예약
- 요일/시간 기반 반복 일정
- 반복 예약 생성
- 충돌 처리
- 수정/삭제

### Phase 6 — 포인트 추첨
- 포인트 보유
- 추첨 신청
- 가중치 기반 추첨
- 당첨/탈락 처리
- 포인트 차감/반환
- 결과 저장

이 단계의 핵심 로직은 사용자가 반드시 설명할 수 있어야 한다.

### Phase 7 — 리뷰와 통계
- 이용 완료 사용자만 리뷰
- 평균 평점
- 이용 횟수
- 경쟁률
- 인기 좌석 통계
- 시간대/요일별 이용률

### Phase 8 — 대기열
- 대기 신청
- 예약 취소 시 후속 처리
- 자동 재배정
- 우선순위 정책

### Phase 9 — 동시성 문제 재현 및 해결

AI를 이용해 동시 요청 테스트를 작성하고 실제 중복 예약 문제를 재현한다.

이후 다음을 비교한다.
- DB 제약조건
- 트랜잭션
- 낙관적 락
- 비관적 락

사용자는 “왜 문제가 발생했는지”와 “왜 이 해결책을 선택했는지”를 설명할 수 있어야 한다.

### Phase 10 — 성능 개선
- 느린 쿼리 찾기
- 인덱스 적용
- 실행 계획 확인
- 집계 쿼리 개선
- Redis 캐시
- 캐시 무효화 정책

### Phase 11 — 테스트 및 문서화
- 단위 테스트
- 통합 테스트
- 예약 로직 테스트
- 추첨 로직 테스트
- 동시성 테스트
- Swagger / OpenAPI
- GitHub README

### Phase 12 — 배포
- Docker
- 애플리케이션 컨테이너화
- MySQL / Redis 환경 구성
- 클라우드 배포
- 외부 접속 가능한 서비스 구축

---

## 11. AI 사용 원칙

### 원칙 1 — AI가 먼저 구현해도 된다
시간 절약을 위해 AI가 현재 단계의 코드를 직접 작성하는 것을 허용한다. 단, 한 번에 모든 Phase를 구현하지 않는다.

### 원칙 2 — 구현 후 반드시 설명한다
AI는 코드 생성 후 다음을 설명해야 한다.
- 어떤 파일을 만들거나 수정했는가
- 각 파일은 왜 필요한가
- 요청이 들어오면 어떤 순서로 실행되는가
- 현재 단계에서 반드시 알아야 할 개념은 무엇인가
- 지금은 몰라도 되는 내용은 무엇인가

### 원칙 3 — 사용자는 모든 코드를 처음부터 작성할 필요가 없다
사용자의 목표는 타이핑 연습이 아니라 **백엔드 프로젝트를 빠르게 완성하면서 이해도를 확보하는 것**이다.

반복적인 보일러플레이트는 AI에게 맡길 수 있다. 대신 핵심 비즈니스 로직과 기술적 의사결정은 이해해야 한다.

### 원칙 4 — 설명할 수 없는 코드는 방치하지 않는다
특히 다음은 사용자가 설명할 수 있는 상태로 만든다.
- Entity 관계
- Service 로직
- 예약 충돌
- 트랜잭션
- 동시성
- 추첨
- 인증
- 인덱스
- Redis

### 원칙 5 — AI 생성 코드를 그대로 신뢰하지 않는다
AI가 작성한 코드도 다음을 확인한다.
- 실제 빌드되는가
- 테스트가 통과하는가
- 요구사항을 만족하는가
- 불필요하게 복잡하지 않은가
- 사용하지 않는 기술을 억지로 추가하지 않았는가

---

## 12. 포트폴리오 개발 원칙

### 완성 속도를 중요하게 생각한다
완벽한 사전 학습보다 작동하는 결과물을 빠르게 만든다.

### 그러나 이해를 버리지는 않는다
AI가 만든 결과물을 단순 복사해서 제출하는 프로젝트가 되지 않도록 한다.

### 기능 수보다 문제 해결 경험을 우선한다

```text
문제 발견
→ 문제 재현
→ 원인 분석
→ 해결책 비교
→ 해결책 적용
→ 테스트/측정
→ 결과 기록
```

이 흐름을 프로젝트 안에 남긴다.

### 기술 선택에는 이유가 있어야 한다
면접에서 “왜 이 기술을 썼나요?”에 답할 수 있어야 한다.

---

## 13. 각 단계의 완료 기준

각 Phase는 단순히 “코드가 동작한다”에서 끝나지 않는다.

최소 완료 조건:
1. 애플리케이션이 실행된다.
2. 해당 기능이 실제로 동작한다.
3. 테스트 또는 직접 요청으로 확인했다.
4. 사용자가 주요 코드 흐름을 설명할 수 있다.
5. 핵심 설계 이유를 대략 설명할 수 있다.
6. Git에 의미 있는 단위로 커밋한다.

단, 프레임워크 내부 구현까지 완벽히 이해해야 다음 단계로 넘어가는 것은 아니다.

---

## 14. 최종 포트폴리오 목표

프로젝트 완성 후 다음 질문에 답할 수 있어야 한다.

1. SeatFlow는 어떤 문제를 해결하기 위해 만든 서비스인가?
2. 일반 예약 시스템과 무엇이 다른가?
3. DB를 왜 이런 구조로 설계했는가?
4. 시간 중복 예약은 어떻게 판정하는가?
5. 동일 좌석에 요청이 동시에 들어오면 어떤 문제가 발생하는가?
6. 그 문제를 어떻게 재현했는가?
7. 어떤 동시성 제어 방식을 선택했고 왜 선택했는가?
8. 포인트 기반 좌석 추첨은 어떻게 동작하는가?
9. 추첨 공정성을 어떻게 고려했는가?
10. 반복 예약은 어떻게 설계했는가?
11. 어떤 쿼리가 느렸는가?
12. 인덱스를 왜 적용했는가?
13. Redis를 왜 사용했는가?
14. 캐시 데이터 일관성은 어떻게 관리했는가?
15. AI는 프로젝트 개발 과정에서 어떤 부분에 사용했는가?
16. AI가 생성한 코드의 정확성을 어떻게 검증했는가?
17. 프로젝트에서 가장 어려웠던 기술적 문제는 무엇이었는가?
18. 그 문제를 어떻게 분석하고 해결했는가?

최종적으로 **AI를 적극적으로 활용해 개발 속도를 높이면서도, 프로젝트를 자신의 말로 설명하고 유지보수할 수 있는 신입 백엔드 개발자 수준**에 도달하는 것을 목표로 한다.
